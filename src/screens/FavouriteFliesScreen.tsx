import { View } from 'react-native';
import FlyListItem from '~components/FlyListItem';

const FavouriteFliesScreen: React.FC = () => {
  return (
    <View>
    </View>
  );
};

export default FavouriteFliesScreen;
